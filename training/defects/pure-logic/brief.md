# Pure logic defect

The parity predicate has been changed so the test suite receives the opposite
answer for ordinary integers. Inspect the unit assertion and the smallest
counterexample before looking at the patch.
